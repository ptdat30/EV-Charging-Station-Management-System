# ===============================================================
# FILE: application.yml
# LOCATION: api-gateway/src/main/resources/application.yml
# MỤC ĐÍCH: Local overrides (ưu tiên thấp hơn Config Server)
# ===============================================================

# Chỉ giữ những config CẦN override local
# Phần lớn config sẽ lấy từ Config Server

logging:
  level:
    root: INFO
    org.springframework.cloud.gateway: DEBUG