# ===============================================================
# FILE: application.yml
# LOCATION: api-gateway/src/main/resources/application.yml
# MỤC ĐÍCH: Local overrides only (chủ yếu lấy từ Config Server)
# ===============================================================

spring:
  codec:
    max-in-memory-size: 10MB

logging:
  level:
    root: INFO
    org.springframework.cloud.gateway: DEBUG