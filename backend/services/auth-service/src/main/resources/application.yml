# FILE: application.yml (in auth-service/src/main/resources)

spring:
  application:
    name: auth-service # Name registered on Eureka

  config:
    import: "optional:configserver:http://localhost:8888" # Connect to Config Server

# Default local port (can be overridden by Config Server)
server:
  port: 8086

jwt:
  secret: f4f54e19a4e3b5e1b2e6a3d7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7
  expirationMs: 86400000