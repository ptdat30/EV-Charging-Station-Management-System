server:
  port: 8081 # Chạy station-service trên một cổng khác

spring:
  application:
    name: STATION-SERVICE  # Phải khớp với tên trong API Gateway route
  datasource:
    url: jdbc:mysql://localhost:3306/station_service_db?useSSL=false
    username: root # Thay bằng username của bạn
    password: huynhdat123@ # Thay bằng mật khẩu của bạn
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    non-secure-port: 8081
    instance-id: ${spring.application.name}:${server.port}